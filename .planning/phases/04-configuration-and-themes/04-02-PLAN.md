---
phase: 04-configuration-and-themes
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app.rs
  - src/editor.rs
  - src/status_bar.rs
  - src/highlighter.rs
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05]

must_haves:
  truths:
    - "Divider color comes from theme.divider_fg, not hardcoded DarkGray"
    - "Status bar colors come from theme, not hardcoded DarkGray/White"
    - "Confirm prompt uses theme.confirm_bg/fg, not hardcoded Red/White"
    - "Filename prompt and search prompt use theme.prompt_bg/fg, not hardcoded Blue/White"
    - "Editor line numbers use theme.line_number_fg, not hardcoded DarkGray"
    - "Selection overlay uses theme.selection_bg, not hardcoded Rgb(68,68,102)"
    - "Search highlights use theme.search_active_*/search_match_*, not hardcoded Cyan/Yellow"
    - "Syntect highlighter uses theme.syntect_theme name, not hardcoded base16-ocean.dark"
    - "All color changes are visible when switching themes"
  artifacts:
    - path: "src/app.rs"
      provides: "Theme-aware rendering for divider, confirm prompt, filename prompt, search prompt"
      contains: "self.theme"
    - path: "src/editor.rs"
      provides: "Theme-aware editor with themed line numbers, selection, search highlights"
      contains: "self.theme"
    - path: "src/status_bar.rs"
      provides: "Theme-aware status bar"
      contains: "theme"
    - path: "src/highlighter.rs"
      provides: "Theme-configurable syntect highlighter"
      contains: "theme_name"
  key_links:
    - from: "src/app.rs"
      to: "src/theme.rs"
      via: "self.theme field used in render()"
      pattern: "self\\.theme\\."
    - from: "src/app.rs"
      to: "src/editor.rs"
      via: "Editor::new takes &Theme"
      pattern: "Editor::new.*theme"
    - from: "src/app.rs"
      to: "src/status_bar.rs"
      via: "StatusBar::render takes &Theme"
      pattern: "status_bar.*render.*theme"
    - from: "src/app.rs"
      to: "src/highlighter.rs"
      via: "MarkdownHighlighter::new takes syntect_theme name"
      pattern: "MarkdownHighlighter::new.*theme"
---

<objective>
Wire the Theme through all rendering components — replace every hardcoded `Color::` literal in the codebase with theme field references. After this plan, switching the `theme` config value changes all colors in the editor.

Purpose: Complete the theming system by connecting Plan 01's Theme struct to all rendering code.
Output: All 4 source files updated to use theme colors instead of hardcoded values.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-configuration-and-themes/4-CONTEXT.md
@.planning/phases/04-configuration-and-themes/04-01-SUMMARY.md
@src/theme.rs
@src/config.rs
@src/app.rs
@src/editor.rs
@src/status_bar.rs
@src/highlighter.rs

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From src/theme.rs (created in Plan 01):
```rust
pub struct Theme {
    pub editor_bg: Option<Color>,
    pub editor_fg: Option<Color>,
    pub line_number_fg: Color,
    pub selection_bg: Color,
    pub search_active_bg: Color,
    pub search_active_fg: Color,
    pub search_match_bg: Color,
    pub search_match_fg: Color,
    pub divider_fg: Color,
    pub status_bar_bg: Color,
    pub status_bar_fg: Color,
    pub confirm_bg: Color,
    pub confirm_fg: Color,
    pub prompt_bg: Color,
    pub prompt_fg: Color,
    pub syntect_theme: String,
}
```

From src/config.rs (created in Plan 01):
```rust
pub enum EditingMode { Vim, Nano }
```

From src/app.rs (modified in Plan 01):
```rust
pub struct App<'a> {
    // ... existing fields ...
    pub theme: theme::Theme,
    pub editing_mode: config::EditingMode,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire theme into Editor and MarkdownHighlighter</name>
  <files>src/editor.rs, src/highlighter.rs</files>
  <read_first>
    - src/editor.rs (full file — understand current Color:: usage)
    - src/highlighter.rs (full file — understand syntect theme usage)
    - src/theme.rs (Theme struct from Plan 01)
  </read_first>
  <action>
1. Update `src/highlighter.rs`:

   a. Change `MarkdownHighlighter::new()` signature to `pub fn new(syntect_theme_name: &str) -> Self`. Instead of hardcoding `"base16-ocean.dark"`, look up `ts.themes.get(syntect_theme_name)`. If the name is not found, fall back to `"base16-ocean.dark"` with an eprintln warning.

   b. Change `line_number_span()` to accept a `line_number_fg: Color` parameter instead of hardcoding `Color::DarkGray`. New signature: `pub fn line_number_span(row: usize, total_lines: usize, line_number_fg: Color) -> Span<'static>`. Replace `Style::default().fg(Color::DarkGray)` with `Style::default().fg(line_number_fg)`.

2. Update `src/editor.rs`:

   a. Add `use crate::theme::Theme;` import.

   b. Add a `theme: Theme` field to the `Editor` struct (owned, cloned from the App's theme).

   c. Change `Editor::new()` signature to accept `theme: Theme` as a new parameter. Store it in the struct. Use `theme.line_number_fg` in the `textarea.set_line_number_style()` call instead of hardcoded `Color::DarkGray`. Pass `&theme.syntect_theme` to `MarkdownHighlighter::new()`.

   d. In `render_highlighted()`:
      - Replace the selection_style line (currently `Style::default().bg(Color::Rgb(68, 68, 102))`) with `Style::default().bg(self.theme.selection_bg)`.
      - Replace the active search match style (currently `Style::default().bg(Color::Cyan).fg(Color::Black)`) with `Style::default().bg(self.theme.search_active_bg).fg(self.theme.search_active_fg)`.
      - Replace the other-match style (currently `Style::default().bg(Color::Yellow).fg(Color::Black)`) with `Style::default().bg(self.theme.search_match_bg).fg(self.theme.search_match_fg)`.
      - In the `line_number_span` call, pass `self.theme.line_number_fg` as the new parameter.

   e. Ensure `App::new()` in app.rs passes `theme.clone()` when constructing `Editor::new()`. (This will be done in Task 2 when wiring app.rs, but Editor::new must already accept the parameter.)
  </action>
  <verify>
    <automated>cd /Users/sho_luv/home/projects/mine/mdedit && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/highlighter.rs `MarkdownHighlighter::new` accepts `syntect_theme_name: &str` parameter
    - src/highlighter.rs does NOT contain the string `"base16-ocean.dark"` as the primary theme (only as fallback)
    - src/highlighter.rs `line_number_span` accepts `line_number_fg: Color` parameter
    - src/highlighter.rs does NOT contain `Color::DarkGray` as a hardcoded value in line_number_span
    - src/editor.rs contains `theme: Theme` field in Editor struct
    - src/editor.rs `Editor::new` accepts `theme: Theme` parameter
    - src/editor.rs does NOT contain `Color::Rgb(68, 68, 102)` (replaced by self.theme.selection_bg)
    - src/editor.rs does NOT contain `Color::Cyan` (replaced by self.theme.search_active_bg)
    - src/editor.rs does NOT contain `Color::Yellow` (replaced by self.theme.search_match_bg)
    - src/editor.rs contains `self.theme.selection_bg`
    - src/editor.rs contains `self.theme.search_active_bg`
    - src/editor.rs contains `self.theme.line_number_fg`
    - `cargo check` succeeds (NOTE: may have temporary errors in app.rs until Task 2 updates the App::new call — if so, temporarily adjust App::new call to compile)
  </acceptance_criteria>
  <done>Editor and highlighter use theme colors for all rendering. No hardcoded Color:: values remain in editor.rs or highlighter.rs (except Color::Black in the cursor underline which is structural, not themed). cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire theme into App and StatusBar rendering</name>
  <files>src/app.rs, src/status_bar.rs</files>
  <read_first>
    - src/app.rs (full file — understand render() method and all Color:: usage)
    - src/status_bar.rs (full file — understand render method)
    - src/theme.rs (Theme struct)
    - src/editor.rs (updated Editor::new signature from Task 1)
  </read_first>
  <action>
1. Update `src/status_bar.rs`:

   a. Add `use crate::theme::Theme;` import.

   b. Change `StatusBar::render()` to accept an additional `theme: &Theme` parameter. New signature:
      ```rust
      pub fn render(&self, frame: &mut Frame, area: Rect, filename: &str, cursor: (usize, usize), modified: bool, theme: &Theme)
      ```

   c. Replace the hardcoded `Style::default().bg(Color::DarkGray).fg(Color::White)` with `Style::default().bg(theme.status_bar_bg).fg(theme.status_bar_fg)`.

   d. Remove the `use ratatui::style::Color;` import if it's no longer needed (Style is still needed).

2. Update `src/app.rs`:

   a. In `App::new()`, pass `theme.clone()` to `Editor::new()`:
      Change `editor: Editor::new(content, filepath)` to `editor: Editor::new(content, filepath, theme.clone())`.

   b. In `render()` — Split mode divider: replace `Style::default().fg(Color::DarkGray)` with `Style::default().fg(self.theme.divider_fg)`.

   c. In `render()` — ConfirmQuit prompt: replace `Style::default().bg(Color::Red).fg(Color::White)` with `Style::default().bg(self.theme.confirm_bg).fg(self.theme.confirm_fg)`.

   d. In `render()` — PromptFilename bar: replace `Style::default().bg(Color::Blue).fg(Color::White)` with `Style::default().bg(self.theme.prompt_bg).fg(self.theme.prompt_fg)`.

   e. In `render()` — Search bar: replace `Style::default().bg(Color::Blue).fg(Color::White)` with `Style::default().bg(self.theme.prompt_bg).fg(self.theme.prompt_fg)`.

   f. In `render()` — StatusBar::render call: pass `&self.theme` as the new last parameter:
      Change `self.status_bar.render(frame, status_area, ...)` to `self.status_bar.render(frame, status_area, ..., &self.theme)`.

   g. Remove any now-unused `Color` imports from app.rs if all Color:: references are gone (likely `Color` can be removed from the `use ratatui::style::{Color, Style}` line, keeping only `Style`).

3. Final verification: grep the entire `src/` directory for any remaining hardcoded `Color::` that should be themed. The ONLY acceptable remaining `Color::` usage is in `src/theme.rs` (where themes are defined). All other files should reference `theme.field_name` or `self.theme.field_name`.
  </action>
  <verify>
    <automated>cd /Users/sho_luv/home/projects/mine/mdedit && cargo build 2>&1 | tail -5 && echo "---" && grep -rn "Color::" src/ --include="*.rs" | grep -v "theme.rs" | grep -v "// theme" | grep -v "use.*Color"</automated>
  </verify>
  <acceptance_criteria>
    - src/app.rs does NOT contain `Color::DarkGray` (divider was hardcoded)
    - src/app.rs does NOT contain `Color::Red` (confirm was hardcoded)
    - src/app.rs does NOT contain `Color::Blue` (prompt was hardcoded)
    - src/app.rs contains `self.theme.divider_fg`
    - src/app.rs contains `self.theme.confirm_bg`
    - src/app.rs contains `self.theme.prompt_bg`
    - src/app.rs contains `&self.theme` in the status_bar.render call
    - src/status_bar.rs contains `theme: &Theme` in render signature
    - src/status_bar.rs contains `theme.status_bar_bg`
    - src/status_bar.rs does NOT contain `Color::DarkGray`
    - `cargo build` succeeds with no errors
    - grep for `Color::` in src/ excluding theme.rs returns zero results (or only import lines)
  </acceptance_criteria>
  <done>All hardcoded colors replaced with theme references across app.rs, editor.rs, status_bar.rs, and highlighter.rs. Switching theme in config changes all colors. cargo build succeeds. No Color:: literals remain outside theme.rs.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with zero errors
- `grep -rn "Color::" src/ --include="*.rs" | grep -v theme.rs | grep -v "use "` returns no results
- Theme struct is threaded through App -> Editor, App -> StatusBar, App -> render()
- MarkdownHighlighter accepts configurable syntect theme name
- All 5 CONF requirements are satisfied: config file (CONF-01), theme selection (CONF-02), custom themes (CONF-03), mode setting (CONF-04), color detection (CONF-05)
</verification>

<success_criteria>
- Zero hardcoded Color:: values remain outside src/theme.rs
- Theme flows from config -> App -> all rendering components
- Syntect highlighter uses theme-specified theme name
- Status bar, divider, prompts, search highlights, selection, line numbers all themed
- cargo build succeeds
- Changing `theme = "dracula"` in config would visibly change all colors
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-and-themes/04-02-SUMMARY.md`
</output>
